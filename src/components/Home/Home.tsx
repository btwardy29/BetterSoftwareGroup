import React, { FC, useEffect } from "react";
import api from "../../API";

const Home: FC = () => {
  // useEffect(() => {
  //   api.getMedia();
  // });
  return <div>Home</div>;
};

export default Home;
